name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: |
          echo "hello world"
          echo "this is the first step of the first job"
      - name: Print bye
        run: | 
          ls -al
          echo "Done - Bye!"
      
        
